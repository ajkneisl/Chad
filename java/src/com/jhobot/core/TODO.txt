beta-tree version -> unstable-0.2

current to do -> 
  # put main files in core ()
  # do file checking before json requesting
  # make russian roulette again
  # make tic tac toe
  # clean up code
  # make ban and kick work better
  # fix bug report (instead of changing all ids, change none, just add a var of "lastid")
